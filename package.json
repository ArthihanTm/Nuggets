{
  "name": "nuggets",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && npm run build:server --prefix server",
    "start": "node server/dist/index.js"
  }
}
